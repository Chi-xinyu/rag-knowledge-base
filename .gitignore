# ===== Git 忽略文件：声明哪些文件/目录不要提交进版本库 =====
# 这些文件都是"本机生成、因人而异"的中间产物，不该进 Git。
# 如果你还没用 Git 可以不管；开始用 Git 后它自动生效。

# IDEA 编辑器配置（每台电脑不同，不提交）
.idea/
*.iml

# Maven 下载的本地依赖仓库（体积巨大，绝不提交）
target/

# 日志文件
*.log

# 系统文件
.DS_Store
Thumbs.db

# 敏感配置（第 3 步接 API 后，Key 要放这里并加入 .gitignore，绝不提交）
# application-local.yml
